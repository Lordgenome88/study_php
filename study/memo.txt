 
基本の連想配列作成
連想配列の受け渡し、表示
入れ子の連想配列作成
入れ子の連想配列受け渡し、表示→foreach取り出しのつくり方
-------------------------------とりあえずここまでできれば結構安心



階層情報サンプルで入れ子連想配列のつくり方
→データの解析、手入れ

方法1
　新規IDを作成した新規作成コンテンツを配列で覚えておく
方法2
　コンテンツを編集する段階で親名を覚えておく


削除の仕組みをPHP側で行う
ルールとして親は先に登録する
新旧比較連想配列作成
array = {};

for文開始

if(新規追加フラグ){
  別連想配列に保存 array{oldID: "1"}
  コンテンツマスタークリエイト！
  現在のID取得(↑のやつのID)
  現在のIDを↑の配列に「新ID」みたいな項目で持たせる array{oldID: "1",newID:"398"}
}

for(全コンテンツ.length){
    for(array.length){
      if(自分のparentIDとarray.oldIDの値が同じとき){
     自分のparentIDをarray.newIDで上書きする
    }
  }
}
for文終了

ParentIDだけじゃなくてParentNameも覚えておけばいい